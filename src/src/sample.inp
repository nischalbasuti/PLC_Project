boolean run = true;

int prev = 0;
int curr = 1;
int temp = 1;

char str = "";

while run do
begin
    str = str + ", " + prev;
    temp = curr;
    curr = prev + curr;
    prev = temp;
    if curr > 20 then
    begin
        run = false;
    end;
end;

print str;